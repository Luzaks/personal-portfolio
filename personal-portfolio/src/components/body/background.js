import React from 'react';
import { Back, Line } from '../../styles/StyledComponents';

const Background = () => {
  return(
    <Back className="background">
      <Line className="line" />
      <Line className="line" />
      <Line className="line" />
    </Back>   
  );
};

export default Background;
